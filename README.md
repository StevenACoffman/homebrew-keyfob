# Install [keyfob](http://github.com/StevenACoffman/keyfob) cli with brew

It is that simple :

```shell
brew tap StevenACoffman/keyfob
brew install keyfob

```

Release should get picked up automatically with `brew upgrade`
